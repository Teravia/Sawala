// Reputation/GRP scoring configuration.
// Keep all thresholds here — never hardcode in UI or business logic.
export const reputationConfig = {
  validContributionsRequiredForGrpEligibility: 1000,
  grpDailyLimit: undefined, // TODO
  grpCooldownSeconds: undefined, // TODO
};
